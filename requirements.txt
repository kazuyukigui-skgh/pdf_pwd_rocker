# PDF Locker - 依存パッケージ

# PDF操作（AES暗号化サポート付き）
pypdf[crypto]>=4.0.0

# ドラッグ&ドロップ機能
tkinterdnd2>=0.3.0

# Office文書→PDF変換（すべてMIT License - 商用利用可能）
docx2pdf>=0.1.8  # Word文書変換（Windows/macOS）
comtypes>=1.2.0; sys_platform == 'win32'  # Excel/PowerPoint変換（Windows専用）

# exe化ツール（開発時のみ必要）
pyinstaller>=6.0.0
